# Python caches
__pycache__/
*.pyc

# Large generated outputs (regenerable from the solver)
nereid_anim/
2/
*.npz

# Keep nereid_output text artifacts (calibration.json, perth_validation.md,
# csv, json, run.log) but not the heavy binary field dumps (*.npz above).

# Solver working dir (calibration.json is mirrored into validation/)
nereid_output/

# User's separate dissertation project — scratch, never tracked
ignore/

# Transient push transcript (was never tracked)
validation/push.log
